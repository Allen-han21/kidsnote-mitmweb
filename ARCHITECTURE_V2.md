# Kidsnote mitmweb - 아키텍처 설계 v2.0

> **기반 분석**: allproxy/mitmproxy-ui 프로젝트 패턴 적용

## 📊 3-Way 아키텍처 비교

### 비교표

| 구분 | 원본 mitmproxy | allproxy/mitmproxy-ui | **kidsnote-mitmweb** |
|------|---------------|----------------------|---------------------|
| **핵심 전략** | Python 올인원 | Node.js 재구현 | **Python + 현대적 UI** |
| **프록시** | mitmproxy core | mockttp (JS) | **mitmproxy core** ✅ |
| **백엔드** | Tornado | Node.js + Socket.IO | **Tornado + Socket.IO** |
| **프론트엔드** | React 17 + Redux | React 17 + MobX | **React 19 + Zustand** |
| **실시간 통신** | WebSocket | Socket.IO | **Socket.IO** ✅ |
| **빌드 도구** | - | CRA | **Vite** |
| **탭 관리** | 단일 세션 | 멀티 탭 ✅ | **멀티 탭** ✅ |
| **Breakpoint** | 제한적 | 강력한 UI ✅ | **강력한 UI** ✅ |
| **프로토콜** | HTTP/WS | +gRPC/MongoDB/Redis | **HTTP/WS** |
| **확장성** | Addon | 독립 구현 | **Addon + 커스텀 UI** |

---

## 🏗️ 최종 아키텍처 설계

### 전체 시스템 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                         iOS Simulator                            │
│                    Proxy: 127.0.0.1:8080                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ↓ HTTP/HTTPS
┌─────────────────────────────────────────────────────────────────┐
│                    Python mitmproxy Core                         │
│                     (변경 없음 - 안정성)                         │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Proxy Engine                                              │ │
│  │  - HTTP/HTTPS MITM                                         │ │
│  │  - SSL/TLS Handling                                        │ │
│  │  - WebSocket Support                                       │ │
│  │  - Flow Management                                         │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Custom Addons (신규 개발)                                 │ │
│  │                                                            │ │
│  │  1. SocketIOEmitter                                        │ │
│  │     - flow 이벤트를 Socket.IO로 emit                       │ │
│  │     - 실시간 UI 업데이트                                   │ │
│  │                                                            │ │
│  │  2. KidsnoteAnalyzer                                       │ │
│  │     - Kidsnote API 특화 분석                              │ │
│  │     - 메트릭 계산                                          │ │
│  │     - 에러 패턴 감지                                       │ │
│  │                                                            │ │
│  │  3. BreakpointManager                                      │ │
│  │     - 요청/응답 인터셉트                                   │ │
│  │     - UI에서 수정 대기                                     │ │
│  │     - 수정 후 재전송                                       │ │
│  │                                                            │ │
│  │  4. SessionManager                                         │ │
│  │     - 멀티 탭 세션 관리                                    │ │
│  │     - 세션별 flow 격리                                     │ │
│  │     - 세션 저장/복원                                       │ │
│  └────────────────────────────────────────────────────────────┘ │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│              Tornado Web Server (8081)                           │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  REST API (기존 + 확장)                                    │ │
│  │                                                            │ │
│  │  기존 mitmproxy API:                                       │ │
│  │  - GET  /flows                                             │ │
│  │  - GET  /flows/{id}                                        │ │
│  │  - POST /flows/{id}/resume                                 │ │
│  │                                                            │ │
│  │  신규 API:                                                 │ │
│  │  - GET  /api/sessions                (세션 목록)          │ │
│  │  - POST /api/sessions                (새 세션)            │ │
│  │  - GET  /api/sessions/{id}/flows     (세션별 flow)        │ │
│  │  - POST /api/breakpoint/{id}/modify  (요청 수정)          │ │
│  │  - GET  /api/metrics/summary         (통계 요약)          │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Socket.IO Server (신규)                                   │ │
│  │                                                            │ │
│  │  Events (Server → Client):                                │ │
│  │  - 'flow:add'       새 flow 추가                          │ │
│  │  - 'flow:update'    flow 업데이트                         │ │
│  │  - 'flow:remove'    flow 삭제                             │ │
│  │  - 'metrics:update' 메트릭 업데이트                       │ │
│  │  - 'breakpoint'     breakpoint 발생                       │ │
│  │                                                            │ │
│  │  Events (Client → Server):                                │ │
│  │  - 'subscribe:{sessionId}'  세션 구독                     │ │
│  │  - 'breakpoint:continue'    breakpoint 계속               │ │
│  │  - 'breakpoint:modify'      요청 수정                     │ │
│  │  - 'flow:resend'            요청 재전송                   │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Static Files                                              │ │
│  │  - /static/ (빌드된 React 앱)                             │ │
│  └────────────────────────────────────────────────────────────┘ │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ↓ HTTP + Socket.IO
┌─────────────────────────────────────────────────────────────────┐
│                    React 19 SPA (Vite)                           │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  State Management (Zustand)                                │ │
│  │                                                            │ │
│  │  useSessionStore                                           │ │
│  │  ├── sessions: Session[]                                   │ │
│  │  ├── activeSessionId: string                               │ │
│  │  ├── createSession()                                       │ │
│  │  └── switchSession()                                       │ │
│  │                                                            │ │
│  │  useFlowStore                                              │ │
│  │  ├── flowsBySession: Map<sessionId, Flow[]>               │ │
│  │  ├── addFlow()                                             │ │
│  │  ├── updateFlow()                                          │ │
│  │  └── getFlowsForSession()                                  │ │
│  │                                                            │ │
│  │  useMetricsStore                                           │ │
│  │  ├── metricsBySession: Map<sessionId, Metrics>            │ │
│  │  ├── updateMetrics()                                       │ │
│  │  └── calculateMetrics()                                    │ │
│  │                                                            │ │
│  │  useBreakpointStore                                        │ │
│  │  ├── pendingBreakpoints: Breakpoint[]                     │ │
│  │  ├── continueBreakpoint()                                  │ │
│  │  └── modifyAndContinue()                                   │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Socket.IO Client                                          │ │
│  │                                                            │ │
│  │  useEffect(() => {                                         │ │
│  │    socket.on('flow:add', (flow) => {                       │ │
│  │      addFlow(flow);                                        │ │
│  │    });                                                     │ │
│  │    socket.on('metrics:update', (metrics) => {             │ │
│  │      updateMetrics(metrics);                               │ │
│  │    });                                                     │ │
│  │  }, []);                                                   │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  UI Components                                             │ │
│  │                                                            │ │
│  │  Layout/                                                   │ │
│  │  ├── Header                    (탭 + 세션 관리)           │ │
│  │  ├── MainView                  (세션별 컨텐츠)            │ │
│  │  └── StatusBar                 (연결 상태)                │ │
│  │                                                            │ │
│  │  Session/                      ⭐ allproxy 패턴           │ │
│  │  ├── SessionTabs               (멀티 탭 UI)               │ │
│  │  ├── SessionManager            (세션 CRUD)                │ │
│  │  └── SessionExporter           (세션 저장/로드)           │ │
│  │                                                            │ │
│  │  Flow/                                                     │ │
│  │  ├── FlowTable                 (flow 목록)                │ │
│  │  ├── FlowDetail                (상세 뷰)                  │ │
│  │  ├── RequestViewer             (요청 뷰어)                │ │
│  │  └── ResponseViewer            (응답 뷰어)                │ │
│  │                                                            │ │
│  │  Metrics/                      ⭐ 우리 고유 기능          │ │
│  │  ├── MetricsPanel              (대시보드)                 │ │
│  │  ├── ResponseTimeChart         (응답시간 차트)            │ │
│  │  ├── StatusCodeChart           (상태코드 분포)            │ │
│  │  └── DomainStatsChart          (도메인 통계)              │ │
│  │                                                            │ │
│  │  Breakpoint/                   ⭐ allproxy 패턴           │ │
│  │  ├── BreakpointDialog          (인터셉트 UI)              │ │
│  │  ├── RequestEditor             (요청 수정)                │ │
│  │  └── BreakpointRules           (규칙 관리)                │ │
│  │                                                            │ │
│  │  Filter/                       ⭐ allproxy 참고           │ │
│  │  ├── FilterBar                 (복합 필터)                │ │
│  │  ├── QuickFilters              (빠른 필터)                │ │
│  │  └── SavedFilters              (저장된 필터)              │ │
│  │                                                            │ │
│  │  Kidsnote/                     ⭐ 특화 기능               │ │
│  │  ├── KidsnoteAPIAnalyzer       (Kidsnote API 분석)       │ │
│  │  ├── ErrorTracker              (에러 추적)                │ │
│  │  └── PerformanceReport         (성능 리포트)              │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                             │
                             ↓ Browser
                    http://localhost:8081
```

---

## 🔑 핵심 설계 결정

### 1. mitmproxy Core 유지 ✅

**결정**: Python mitmproxy core를 그대로 사용

**이유**:
- ✅ 검증된 안정성
- ✅ 풍부한 Addon 생태계
- ✅ SSL/TLS MITM 완벽 지원
- ✅ 지속적인 업스트림 업데이트

**대안 거부**: allproxy처럼 Node.js 재구현
- ❌ 개발 복잡도 극대화
- ❌ 안정성 검증 필요
- ❌ 유지보수 부담

### 2. Socket.IO 실시간 통신 ✅

**결정**: Tornado에 Socket.IO 통합

**이유**:
- ✅ 실시간 flow 업데이트
- ✅ Breakpoint 양방향 통신
- ✅ 멀티 탭 세션 동기화
- ✅ allproxy 패턴 검증됨

**구현 방법**:
```python
# mitmproxy/tools/web/app.py
from socketio import AsyncServer
import asyncio

sio = AsyncServer(async_mode='tornado', cors_allowed_origins='*')

@sio.event
async def connect(sid, environ):
    print(f'Client {sid} connected')

@sio.event
async def subscribe(sid, session_id):
    await sio.enter_room(sid, session_id)
```

### 3. Zustand 상태 관리 ✅

**결정**: Redux 대신 Zustand 사용

**이유**:
- ✅ 간단한 API (Redux Toolkit보다 쉬움)
- ✅ TypeScript 친화적
- ✅ React 19 최적화
- ✅ 보일러플레이트 최소화

**MobX 대신 Zustand**:
- allproxy는 MobX 사용
- MobX: 러닝 커브 높음
- Zustand: 배우기 쉽고 효과적

### 4. 멀티 탭 세션 ✅

**결정**: allproxy 패턴 채택

**이유**:
- ✅ 여러 테스트 시나리오 동시 진행
- ✅ 세션별 독립 필터
- ✅ 세션 저장/복원

**구현**:
```typescript
interface Session {
  id: string;
  name: string;
  createdAt: number;
  flows: Flow[];
  filters: FilterExpression;
}

const useSessionStore = create<SessionState>((set) => ({
  sessions: [{ id: 'default', name: 'Main', flows: [], filters: '' }],
  activeSessionId: 'default',
  createSession: (name) => {
    const newSession = { id: uuid(), name, flows: [], filters: '' };
    set((state) => ({
      sessions: [...state.sessions, newSession],
      activeSessionId: newSession.id
    }));
  }
}));
```

### 5. Breakpoint 시스템 ✅

**결정**: allproxy 패턴 참고

**UI 흐름**:
```
1. 사용자가 breakpoint 규칙 설정
   ↓
2. mitmproxy addon이 flow 인터셉트
   ↓
3. Socket.IO로 'breakpoint' 이벤트 emit
   ↓
4. React UI에 BreakpointDialog 표시
   ↓
5. 사용자가 수정 또는 계속
   ↓
6. Socket.IO로 'breakpoint:continue' 또는 'breakpoint:modify'
   ↓
7. addon이 flow 재개 또는 수정 후 재개
```

---

## 📁 프로젝트 구조

```
kidsnote-mitmweb/
├── mitmproxy/                      # 포크한 mitmproxy
│   ├── mitmproxy/
│   │   ├── addons/                 # 기존 addon
│   │   └── kidsnote/               # ⭐ 신규 addon
│   │       ├── socketio_emitter.py
│   │       ├── kidsnote_analyzer.py
│   │       ├── breakpoint_manager.py
│   │       └── session_manager.py
│   │
│   ├── mitmproxy/tools/web/
│   │   ├── app.py                  # Socket.IO 통합
│   │   └── static/                 # 빌드된 React 앱
│   │
│   └── web/                        # 프론트엔드
│       ├── src/
│       │   ├── js/
│       │   │   ├── components/
│       │   │   │   ├── (기존)
│       │   │   │   └── Kidsnote/   # ⭐ 커스텀 컴포넌트
│       │   │   │       ├── Session/
│       │   │   │       │   ├── SessionTabs.tsx
│       │   │   │       │   ├── SessionManager.tsx
│       │   │   │       │   └── SessionExporter.tsx
│       │   │   │       ├── Metrics/
│       │   │   │       │   ├── MetricsPanel.tsx
│       │   │   │       │   ├── ResponseTimeChart.tsx
│       │   │   │       │   └── StatusCodeChart.tsx
│       │   │   │       ├── Breakpoint/
│       │   │   │       │   ├── BreakpointDialog.tsx
│       │   │   │       │   ├── RequestEditor.tsx
│       │   │   │       │   └── BreakpointRules.tsx
│       │   │   │       └── Filter/
│       │   │   │           ├── FilterBar.tsx
│       │   │   │           └── SavedFilters.tsx
│       │   │   └── stores/         # ⭐ Zustand stores
│       │   │       ├── useSessionStore.ts
│       │   │       ├── useFlowStore.ts
│       │   │       ├── useMetricsStore.ts
│       │   │       └── useBreakpointStore.ts
│       │   └── css/
│       └── package.json
│
├── docs/
│   ├── ARCHITECTURE.md
│   ├── ARCHITECTURE_V2.md          # 이 파일
│   ├── ROADMAP.md
│   └── ALLPROXY_ANALYSIS.md        # allproxy 분석 결과
│
├── scripts/
│   ├── dev.sh                      # 개발 서버 시작
│   └── build.sh                    # 프로덕션 빌드
│
└── README.md
```

---

## 🔄 데이터 흐름

### Flow 추가 시나리오

```
1. iOS 앱 → HTTP 요청
   ↓
2. mitmproxy Core → 요청 캡처
   ↓
3. KidsnoteAnalyzer addon → 분석
   ↓
4. SocketIOEmitter addon → Socket.IO emit
   {
     event: 'flow:add',
     data: {
       sessionId: 'default',
       flow: { id, request, response, ... }
     }
   }
   ↓
5. React App → Socket.IO listener
   socket.on('flow:add', (data) => {
     useFlowStore.getState().addFlow(data.flow);
   })
   ↓
6. Zustand Store → 상태 업데이트
   ↓
7. React Component → 자동 리렌더링
```

### Breakpoint 시나리오

```
1. 사용자 → Breakpoint 규칙 설정
   "~u /api/v1/users & ~m POST"
   ↓
2. Socket.IO emit → 'breakpoint:set-rule'
   ↓
3. BreakpointManager addon → 규칙 저장
   ↓
4. mitmproxy → 매칭되는 요청 발생
   ↓
5. BreakpointManager → flow 일시 정지
   ↓
6. Socket.IO emit → 'breakpoint'
   {
     flowId: '12345',
     request: { ... }
   }
   ↓
7. React → BreakpointDialog 표시
   ↓
8. 사용자 → 요청 수정
   ↓
9. Socket.IO emit → 'breakpoint:modify'
   {
     flowId: '12345',
     modifiedRequest: { ... }
   }
   ↓
10. BreakpointManager → flow 재개
```

---

## 🎨 UI/UX 설계

### 메인 레이아웃

```
┌─────────────────────────────────────────────────────────────┐
│  Header                                                      │
│  [Kidsnote mitmweb] [Session: Main ▼] [+ New Session]      │
│  [Capture] [FlowList] [Options] [Metrics] [Breakpoints]    │
├─────────────────────────────────────────────────────────────┤
│  Filter Bar                                                  │
│  [🔍 Search...] [Domain ▼] [Method ▼] [Status ▼] [⚡ Quick]│
├──────────────────────┬──────────────────────────────────────┤
│                      │                                       │
│  Flow List           │  Flow Detail                          │
│  ┌────────────────┐  │  ┌──────────────────────────────┐   │
│  │ POST /api/users│  │  │ Request                      │   │
│  │ 200 OK 234ms   │  │  │ POST /api/v1/users           │   │
│  │                │  │  │ Headers:                     │   │
│  │ GET /api/posts │  │  │   Content-Type: application/… │   │
│  │ 500 Error      │  │  │ Body:                        │   │
│  │                │  │  │   { "name": "..." }          │   │
│  │ ...            │  │  └──────────────────────────────┘   │
│  └────────────────┘  │  ┌──────────────────────────────┐   │
│                      │  │ Response                     │   │
│                      │  │ 200 OK                       │   │
│                      │  │ Headers: ...                 │   │
│                      │  │ Body: { "id": 123, ... }     │   │
│                      │  └──────────────────────────────┘   │
├──────────────────────┴──────────────────────────────────────┤
│  Status Bar                                                  │
│  🟢 Connected | 1,234 requests | 5 errors | 8 breakpoints  │
└─────────────────────────────────────────────────────────────┘
```

### Metrics 탭

```
┌─────────────────────────────────────────────────────────────┐
│  📊 Network Metrics                                          │
├─────────────────────────────────────────────────────────────┤
│  Summary Cards                                               │
│  ┌───────┐ ┌───────┐ ┌───────┐ ┌───────┐                   │
│  │ 1,234 │ │ 2.5%  │ │ 234ms │ │  12   │                   │
│  │Total  │ │Error  │ │  Avg  │ │ Slow  │                   │
│  └───────┘ └───────┘ └───────┘ └───────┘                   │
├─────────────────────────────────────────────────────────────┤
│  Response Time Chart                                         │
│  [📈 Line Chart showing response times over time]           │
├─────────────────────────────────────────────────────────────┤
│  Status Code Distribution      Domain Statistics            │
│  [📊 Pie Chart]                [📊 Bar Chart]                │
└─────────────────────────────────────────────────────────────┘
```

### Breakpoint Dialog

```
┌─────────────────────────────────────────────────────────────┐
│  ⚠️  Breakpoint Hit                                          │
├─────────────────────────────────────────────────────────────┤
│  POST /api/v1/users                                          │
│                                                              │
│  [Request] [Response] [Edit]                                │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ {                                                    │   │
│  │   "name": "John Doe",                                │   │
│  │   "email": "john@example.com"                        │   │
│  │ }                                                    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                              │
│  [Cancel] [Continue] [Modify & Continue]                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 🛠️ 기술 스택 최종 결정

### 백엔드 (Python)

```python
# mitmproxy core
mitmproxy >= 13.0.0

# Socket.IO 통합
python-socketio >= 5.0.0
tornado-socketio >= 0.0.7  # 또는 직접 통합

# 추가 라이브러리
pydantic >= 2.0.0  # 데이터 검증
```

### 프론트엔드 (TypeScript/React)

```json
{
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zustand": "^5.0.0",
    "socket.io-client": "^4.0.0",
    "recharts": "^2.0.0",
    "@mui/material": "^6.0.0",
    "prism-react-renderer": "^2.0.0",
    "react-json-view": "^1.21.3"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "typescript": "^5.0.0",
    "@types/react": "^19.0.0"
  }
}
```

---

## 🚀 개발 로드맵 (수정)

### Phase 1: 기반 구조 (Week 1)

**목표**: Socket.IO 통신 설정

- [ ] Tornado에 Socket.IO 통합
- [ ] SocketIOEmitter addon 개발
- [ ] React에서 Socket.IO client 설정
- [ ] 기본 flow 실시간 업데이트 확인

### Phase 2: 멀티 탭 세션 (Week 2)

**목표**: allproxy 패턴 적용

- [ ] SessionManager addon
- [ ] useSessionStore 구현
- [ ] SessionTabs 컴포넌트
- [ ] 세션별 flow 격리

### Phase 3: Breakpoint 시스템 (Week 3)

**목표**: 요청/응답 인터셉트

- [ ] BreakpointManager addon
- [ ] BreakpointDialog 컴포넌트
- [ ] RequestEditor 컴포넌트
- [ ] Breakpoint 규칙 관리 UI

### Phase 4: Metrics & 분석 (Week 4)

**목표**: 네트워크 분석 기능

- [ ] KidsnoteAnalyzer addon
- [ ] MetricsPanel 실데이터 연결
- [ ] ResponseTimeChart
- [ ] StatusCodeChart
- [ ] DomainStatsChart

### Phase 5: 고급 필터링 (Week 5)

**목표**: allproxy 수준 필터링

- [ ] FilterBar 컴포넌트
- [ ] 복합 쿼리 파서
- [ ] 저장된 필터 관리
- [ ] 빠른 필터 프리셋

### Phase 6: 최적화 & 배포 (Week 6)

**목표**: 프로덕션 준비

- [ ] 성능 최적화
- [ ] Docker 이미지
- [ ] 사용자 문서
- [ ] 테스트 작성

---

## 📊 성공 지표

| 지표 | 목표 | 측정 방법 |
|------|------|----------|
| **실시간성** | < 100ms | Socket.IO 지연시간 |
| **동시 flow** | 10,000+ | 메모리 사용량 < 1GB |
| **세션 전환** | < 50ms | UI 반응 시간 |
| **Breakpoint** | < 200ms | 인터셉트 → UI 표시 |
| **빌드 시간** | < 30초 | Vite 빌드 |
| **번들 크기** | < 500KB | gzip 압축 후 |

---

## 🎯 allproxy vs kidsnote-mitmweb 차이점

| 항목 | allproxy | kidsnote-mitmweb |
|------|----------|------------------|
| **프록시 엔진** | mockttp (재구현) | mitmproxy (검증됨) |
| **안정성** | 자체 검증 필요 | 업스트림 보장 |
| **프로토콜** | 다양 (gRPC, DB) | HTTP/HTTPS 집중 |
| **특화 기능** | 범용 | iOS/Kidsnote 특화 |
| **학습 곡선** | Node.js 전체 | Python addon + React |
| **유지보수** | 독립 프로젝트 | mitmproxy 생태계 |
| **배포** | NPM 패키지 | Docker 이미지 |

---

## 📝 다음 단계

1. **Socket.IO 프로토타입**
   ```bash
   cd mitmproxy
   # Socket.IO 통합 테스트
   ```

2. **allproxy 로컬 실행**
   ```bash
   npm install -g mitmproxy-ui
   mitmproxy-ui
   # UI 패턴 연구
   ```

3. **첫 번째 addon 개발**
   ```python
   # mitmproxy/kidsnote/socketio_emitter.py
   ```

4. **SessionTabs 컴포넌트**
   ```typescript
   // web/src/js/components/Kidsnote/Session/SessionTabs.tsx
   ```

---

## 🔗 참고 자료

- **allproxy 분석**: `./ALLPROXY_ANALYSIS.md`
- **원본 아키텍처**: `./ARCHITECTURE.md`
- **로드맵**: `./ROADMAP.md`
- **mitmproxy Addon 가이드**: https://docs.mitmproxy.org/stable/addons-overview/
- **Socket.IO 문서**: https://socket.io/docs/
- **Zustand 문서**: https://docs.pmnd.rs/zustand/

---

**작성일**: 2025-12-08
**버전**: 2.0
**기반**: allproxy/mitmproxy-ui 분석 결과
